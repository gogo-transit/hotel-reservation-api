name: Deploy
on:
    workflow_dispatch: 
jobs:
    Dev:
      uses: ./.github/workflows/release-template.yaml
      with:
         namespace: "dev"
         deployment: "Devlopement"
         branch: "dev"
         environment: "DEV"
    PROD:
      needs: [Dev]
      uses: ./.github/workflows/release-template.yaml
      with:
         namespace: "dev"
         deployment: "Devlopement"
         branch: "dev"
         environment: "DEV"         
